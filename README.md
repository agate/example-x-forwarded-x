# Example - X-Forwarded-*

## Dependencies

1. Docker
2. Docker Compose

## Start

`docker-compose up`

## Example

`bash run-test`

Please use `nginx.conf` as reference to understand those commands output.
